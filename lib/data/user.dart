class UserData {
  String uid;
  String username;
  String email;
  String firstName;
  String lastName;
  String gender;
  String dob;
  String password;

  UserData({this.uid, this.username, this.email, this.firstName, this.lastName, this.gender, this.dob, this.password});
}